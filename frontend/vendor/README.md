# Local frontend dependencies

Generated by `cd backend && npm run vendor:frontend` from the versions in `backend/package-lock.json`.

- marked 18.0.11 — Markdown parsing; license: MARKED-LICENSE.
- DOMPurify 3.4.14 — HTML sanitization; license: DOMPURIFY-LICENSE.

The browser loads these files from the same origin; no runtime CDN dependency.
